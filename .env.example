# Polymarket wallet (EOA private key). Required for orders, redeem, and wallet endpoints.
POLYMARKET_MAGIC_PK=

# Polygon RPC URL. Optional; defaults to https://polygon-rpc.com
# POLYGON_RPC_URL=https://polygon-rpc.com

# Polymarket Builder API (optional, for builder signing / relay attribution)
# POLYMARKET_BUILDER_API_KEY=
# POLYMARKET_BUILDER_SECRET=
# POLYMARKET_BUILDER_PASSPHRASE=

# NODE_ENV is usually set by the runtime (e.g. Vercel). Use "development" for local dev.
# NODE_ENV=development

# Supabase (optional): persist trade history so it survives refresh and shows in History tab.
# Create table via supabase-schema.sql in Supabase SQL Editor.
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
